namespace ModernFormsNext
{
    /// <summary>
    /// Specifies where tick marks are drawn on a TrackBar.
    /// </summary>
    public enum TickStyle
    {
        None,
        TopLeft,
        BottomRight,
        Both
    }
}
