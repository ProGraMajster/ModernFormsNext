namespace ModernFormsNext.DataBinding
{
    internal static class TrimmingConstants
    {
        internal const string BindingListViewFilterMessage =
            "BindingSource.Filter uses the data source's IBindingListView implementation. " +
            "Filter parsing can depend on members discovered dynamically by the list implementation.";
    }
}
